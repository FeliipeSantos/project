version: '3.8'

services:
  project-dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: project-dev-container
    ports:
      - "8080:8080"
    volumes:
      - ./db.json:/app/db.json
    restart: unless-stopped
